{
  "name": "peer-assessment-backend",
  "version": "1.0.0",
  "description": "Peer Assessment Application Backend",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",               // Start the server
    "dev": "nodemon server.js",              // Start the server in development mode with Nodemon
    "test": "jest --coverage",               // Run tests with coverage report
    "lint": "eslint .",                      // Lint all files
    "prestart": "npm run lint && npm test"   // Run linting and tests before starting the server
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.3",                // Middleware for parsing JSON and URL-encoded data
    "cors": "^2.8.5",                        // Middleware to enable CORS
    "csv-parser": "^3.0.0",                  // Parse CSV files
    "dotenv": "^16.0.3",                     // Load environment variables from .env file
    "express": "^4.21.1",                    // Web framework for Node.js
    "jsonwebtoken": "^9.0.0",                // JWT implementation for authentication
    "multer": "^1.4.5-lts.1",                // Middleware for handling file uploads
    "sqlite3": "^5.1.7"                      // SQLite database integration
  },
  "devDependencies": {
    "eslint": "^8.47.0",                     // JavaScript linting tool
    "jest": "^29.7.0",                       // Testing framework
    "supertest": "^6.3.3",                   // HTTP assertions for testing APIs
    "nodemon": "^3.0.1"                      // Automatically restart the server in development mode
  }
}
