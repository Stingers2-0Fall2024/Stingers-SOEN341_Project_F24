module.exports = {
  testEnvironment: 'node',
  testMatch: ['<rootDir>/backend/**/*.test.js'], // Match test files in any directory under backend
  coverageDirectory: './coverage', // Save coverage reports
};
